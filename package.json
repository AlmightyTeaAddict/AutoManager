{
	"name": "auto-manager",
	"workspaces": [
		"auto-manager",
		"examples/dev",
		"web-dash"
	],
	"scripts": {
		"build-lib": "npm run build -w=auto-manager -w=examples/dev",
		"start-server": "npm run start -w=examples/dev",
		"dev-dash": "npm run dev -w=web-dash",

		"test-lib": "npm run test -w=auto-manager",

		"build": "npm run build -w=auto-manager -w=examples/dev -w=web-dash",
		"format": "npm run format -w=auto-manager -w=examples/dev -w=web-dash"
	}
}
